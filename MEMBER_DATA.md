# 📋 Member Data — autoTOJ

> Справочный документ для AI-агента и всех участников проекта.
> Обновляйте его по мере развития проекта, чтобы агент всегда работал в актуальных рамках.

---

## 1. Stack & Architecture (Техническое ядро)

| Категория            | Технология                                      |
| -------------------- | ----------------------------------------------- |
| **Framework**        | Next.js 16 (App Router)                         |
| **Language**         | TypeScript (Strict mode)                        |
| **Styling**          | Tailwind CSS 4 / Shadcn UI                      |
| **State Management** | Redux Toolkit + RTK Query                       |
| **Database/Backend** | _[Определить: Prisma + PostgreSQL / Supabase / Firebase / другое]_ |

---

## 2. Project Essence (Суть проекта)

| Поле        | Описание                                                                 |
| ----------- | ------------------------------------------------------------------------ |
| **Название**  | autoTOJ                                                                  |
| **Проблема**  | _[Какую задачу решает проект? Например: «Автоматизация подготовки к TOJ-экзамену»]_ |
| **MVP Scope** | _[Что должно быть в первой версии? Например: «Авторизация, генерация тестов, просмотр результатов»]_ |

---

## 3. Coding Standards (Правила написания кода)

### Компоненты

- Использовать **Server Components** по умолчанию.
- Переходить на **Client Components** (`"use client"`) только при необходимости (интерактивность, хуки браузера, Redux и т.д.).

### Структура папок

```
src/
├── app/          # Маршруты, layout, page, loading, error
├── components/   # Переиспользуемые UI-компоненты
│   └── ui/       # Shadcn UI компоненты
├── lib/          # Утилиты, конфиги, helpers
└── hooks/        # Кастомные React-хуки
```

### Типизация

- Строгая типизация для **всех** Props и API-ответов.
- **Никаких `any`** — использовать `unknown` + type guards при необходимости.
- Интерфейсы для данных хранить в `src/lib/types.ts` или рядом с компонентом.

### Обработка ошибок

- Использовать `error.tsx` для маршрутов (Error Boundaries).
- Использовать `loading.tsx` для состояний загрузки (Suspense Boundaries).
- Для API-запросов — обрабатывать ошибки через RTK Query `isError` / `error`.

### Стиль кода

- Именование файлов компонентов: `kebab-case.tsx` (например, `user-card.tsx`).
- Экспорт компонентов: **named export** (не default), кроме `page.tsx` / `layout.tsx`.
- Стили: утилитарные классы Tailwind, при сложной логике — `cn()` (`clsx` + `tailwind-merge`).

---

## 4. Зависимости проекта (текущие)

### Production

| Пакет                    | Версия     |
| ------------------------ | ---------- |
| `next`                   | 16.1.6     |
| `react` / `react-dom`    | 19.2.3     |
| `@reduxjs/toolkit`       | ^2.11.2    |
| `react-redux`            | ^9.2.0     |
| `shadcn`                 | ^3.8.4     |
| `radix-ui`               | ^1.4.3     |
| `lucide-react`           | ^0.563.0   |
| `class-variance-authority` | ^0.7.1   |
| `clsx`                   | ^2.1.1     |
| `tailwind-merge`         | ^3.4.0     |
| `tw-animate-css`         | ^1.4.0     |

### Dev

| Пакет                  | Версия   |
| ---------------------- | -------- |
| `tailwindcss`          | ^4       |
| `@tailwindcss/postcss` | ^4       |
| `typescript`           | ^5       |
| `eslint`               | ^9       |
| `eslint-config-next`   | 16.1.6   |

---

> **⚠️ TODO:** Заполните поля, отмеченные _[курсивом]_, чтобы агент понял полный контекст проекта.
